services:
  mongodb:
    image: mongo:4.4.18
    container_name: mongo-local-dev
    ports:
      - 27017:27017
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data: 